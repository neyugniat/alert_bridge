from loguru import logger


def send_discord(message: str) -> None:
    # Placeholder — real Discord webhook call wired up later, at home
    logger.info(f"[discord placeholder] would send: {message}")