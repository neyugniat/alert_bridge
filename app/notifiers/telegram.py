from loguru import logger


def send_telegram(message: str) -> None:
    # Placeholder — real Telegram Bot API call wired up later, at home
    logger.info(f"[telegram placeholder] would send: {message}")